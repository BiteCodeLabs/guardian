// const row = new MessageActionRow();

//     const embed = new MessageEmbed()
//       .setColor("BLUE")
//       .setTitle(`${member.guild.name}`)
//       .setDescription(`Welcome ${member} to ${member.guild.name}`)
//       .addFields({ name: "Join Message", value: `${message}`, inline: true })
//       .setTimestamp();

//     row.addComponents(
//       new MessageButton()
//         .setCustomId("apply")
//         .setStyle("PRIMARY")
//         .setLabel("Apply")
//         .setEmoji("📜")
//     );

//     return member.send({ embeds: [embed], components: [row] });
// TODO Before running application init command ensure to check if applications are enabled and for all the settings and if they are correct
